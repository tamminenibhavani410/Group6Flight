@{
    Layout = "_Layout";

    string controller = ViewContext.RouteData.Values["controller"]?.ToString() ?? "";
    string action = ViewContext.RouteData.Values["action"]?.ToString() ?? "";
}
<header>
    <nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-light bg-white border-bottom box-shadow mb-3">
        <div class="container-fluid">
            <a class="navbar-brand" asp-area="Airlines" asp-controller="Home" asp-action="Index">
                <img src="~/img/FlightLogo.png" class="me-2" style="height:28px;">Flight
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse" aria-controls="navbarSupportedContent"
                    aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="navbar-collapse collapse d-sm-inline-flex justify-content-between">
                <ul class="navbar-nav flex-grow-1">
                    <li class="nav-item">
                        <a class="nav-item nav-link @(controller == "Home" && action == "Index" ? "active" : "")"
                           asp-area="Airlines" asp-controller="Home" asp-action="Index">DashBoard</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-item nav-link @(controller == "Flights" && action == "ManageFlights" ? "active" : "")"
                           asp-area="Airlines" asp-controller="Flights" asp-action="ManageFlights">Manage Flights</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-item nav-link @(controller == "Flights" && action == "Regulation" ? "active" : "")"
                           asp-area="Airlines" asp-controller="Flights" asp-action="Regulation">Regulation</a>
                    </li>
                </ul>

                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-item nav-link @(controller == "Home" && action == "Index" ? "active" : "")"
                           asp-area="" asp-controller="Home" asp-action="Index">View Site</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
</header>
@if (TempData["Message"] != null)
{
    <div class="alert alert-success pt-5">@TempData["Message"]</div>
}
<main role="main" class="pb-3">
    @RenderBody()
</main>


